services:
  - type: web
    name: daily-meal-plan-viewer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: APPS_SCRIPT_URL
        sync: false
